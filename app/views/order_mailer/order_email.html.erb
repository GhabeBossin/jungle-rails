<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Order confirmation:</h1>
    <p>
      You have successfully completed your order. These items will be shipped to you promptly.
    </p>
    <ul>
      <% @order.line_items.each do |line_item| %>
      <li>
        <%= line_item.product.name %>
        <%= line_item.product.description %>
        <%= line_item.quantity %>
      </li>
    <% end %>
    </ul>
    <p>Your total: <strong><%= (@order.total_cents / 100.0).to_s %></strong></p>
    <p> Thanks for shopping at Jungle.com <%= @url %> </p>
    <p> Have a great day! </p>
  </body>
</html>